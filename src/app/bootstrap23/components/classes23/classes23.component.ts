import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app69-classes23',
  templateUrl: './classes23.component.html',
  styleUrls: ['./classes23.component.scss']
})
export class Classes23Component implements OnInit {

  constructor() { }

  ngOnInit(): void {
    // If bootstrap classes are to be applied... they must be inside class="container"
    // See classes23 component in bootstrap23 module in testing repo
    // <tr class="active">         // other options = warning, 
    // <div class="well">          // Adds a rounded border around an element with a gray background color
  }

}
