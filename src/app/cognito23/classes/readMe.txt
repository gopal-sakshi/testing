It's not good idea to call DynamoDB from Angular(JavaScript). 
It doesn't comes under the AWS best practices. 
The reason is you have to hard code the AccessKey and SecretKey in your Javascript file.

Amplify Command Line Interface (CLI) 
- a unified toolchain to create, integrate, manage the AWS cloud services for your app.


user-pool Id:       see config folder
client Id:          see config folder

